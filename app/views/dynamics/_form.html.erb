<%= form_for(@dynamic) do |f| %>
  <% if @dynamic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dynamic.errors.count, "error") %> prohibited this dynamic from being saved:</h2>

      <ul>
      <% @dynamic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :boards do |board| %>
    <div class="field">
     <%= board.label :name %><br>
     <%= board.text_area :name %><br>
     <%= board.label :descricao %><br>
     <%= board.text_area :descricao %>
    </div>
  <% end -%>


  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_area :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>




